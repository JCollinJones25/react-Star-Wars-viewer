import { Link } from "react-router-dom";

const Movie = () => {
    return (
        <>
        <Link to={`/`}>Back to Home Page</Link>
        <h1> Movie </h1>
        </>
    )
}

export default Movie